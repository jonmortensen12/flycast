# Generated by build-sim.py from index.html — never edit or commit it.
# See README: the physics core is sliced out of the game, not duplicated,
# so a checked-in copy is a copy that can be stale.
sim.mjs
frames.json
